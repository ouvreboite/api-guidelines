formats: ["oas3"]
extends: "spectral:oas"
functions: 
  - isRequiredPropertyDefined
aliases:
  parameters: 
    - $.paths[*].parameters
    - $.paths[*][*].parameters
#rules will be injected from the rules/*.md files
rules: