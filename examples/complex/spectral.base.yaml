formats: ["oas3"]
extends: "spectral:oas"
functions: 
  - isRequiredPropertyDefined
aliases:
  parameters: 
    - $.paths[*].parameters
    - $.paths[*][*].parameters
rules:
#rules will be injected by poltergust